server {
    listen 80;
    server_name _;

    # SPA only — API calls use VITE_API_URL (direct to api service on Railway).
    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}
